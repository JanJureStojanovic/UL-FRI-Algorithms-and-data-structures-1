#include <iostream>
#include <string>
#include <algorithm>
#include <tuple>

using namespace std;

int main() {

    int n, w;
    cin >> n >> w;

    int arr[n][2];

    for (int i = 0; i < n; i++) {
        cin >> arr[i][0] >> arr[i][1];
    }


           
}