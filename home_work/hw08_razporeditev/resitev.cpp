#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

using namespace std;

int main() {

    // Stevilo otrok in stevilo parov
    int n, m;
    cin >> n >> m;

    // Nas data structure
    vector<int, vector<int>> skupina_in_prijatelji;

    // Preberemo m mnogo prijateljstev
    for (int i = 0; i < m; i++) {

    }

}